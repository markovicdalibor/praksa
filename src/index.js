import React from "react";
import ReactDOM from "react-dom";
import { BrowserRouter, Route, Link } from "react-router-dom";
import "./main.sass";
import "./images/logo.png";
import "./images/bbg.png";



if (module.hot) {
    module.hot.accept(
      console.log("Server works!") 
    );
  };